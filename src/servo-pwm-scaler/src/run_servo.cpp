/**
 * Servo aller-retour
 *
 * Turns on an Servo on for one second,
 * then off for one second, repeatedly.
 */

/* #include "Arduino.h"
#include "Servo.h"

int servo_pin = 19;
Servo servo;

void setup()
{
  servo.attach(servo_pin);
  Serial.begin("--- Program is beginning ---");
}

void loop()
{
  servo.write(0);
  delay(1000);
  servo.write(180);
  delay(1000);
} */
